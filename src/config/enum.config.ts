export enum CalculationType {
  Add = 'add',
  Subtract = 'subtract',
  Multiply = 'multiply',
  Divide = 'divide',
}

export enum CalculationRange {
  Ten = 10,
  Hundred = 100,
  Thousand = 1000,
}

export enum CalculationMode {
  Select = 'select',
  Input = 'input',
}
